
/**
 * Servicio de IA eliminado según requerimiento.
 * Para análisis de licitaciones, se recomienda integrar un procesamiento en el backend
 * o utilizar servicios específicos de análisis de documentos.
 */
export const analyzeTender = async (): Promise<null> => {
  return null;
};
